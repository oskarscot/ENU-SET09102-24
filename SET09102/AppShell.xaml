<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:administrator="clr-namespace:SET09102.Administrator.Pages"
       x:Class="SET09102.AppShell">
    <ShellContent Title="Login"
                  ContentTemplate="{DataTemplate administrator:LoginPage}"
                  Route="LoginPage" />
    <ShellContent Title="User Management"
                  ContentTemplate="{DataTemplate administrator:UserManagementPage}"
                  Route="UserManagementPage" />
    <!-- Add other pages as needed -->
</Shell>